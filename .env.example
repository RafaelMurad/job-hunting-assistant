# Database Configuration
# -----------------------
# Production (Neon PostgreSQL with connection pooling):
# DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
#
# Local Development:
# - Recommended: use Neon (free tier) and point DATABASE_URL to a dev branch
# - Alternative: run Postgres locally and point DATABASE_URL to localhost
DATABASE_URL="your_database_url_here"

# Direct database URL (for migrations) - no connection pooling
# Required for Prisma migrations. On Neon, use the non-pooled connection.
DATABASE_URL_UNPOOLED="your_direct_database_url_here"

# =============================================================================
# AUTHENTICATION (NextAuth.js)
# =============================================================================

# NextAuth Secret - REQUIRED
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-nextauth-secret-here"

# GitHub OAuth (for sign-in)
# Create OAuth App: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github (dev)
#               https://your-domain.com/api/auth/callback/github (prod)
AUTH_GITHUB_ID="your-github-client-id"
AUTH_GITHUB_SECRET="your-github-client-secret"

# Google OAuth (for sign-in)
# Create OAuth Client: https://console.cloud.google.com/apis/credentials
# Callback URL: http://localhost:3000/api/auth/callback/google (dev)
#               https://your-domain.com/api/auth/callback/google (prod)
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# =============================================================================
# SOCIAL INTEGRATIONS (GitHub/LinkedIn data sync - separate from auth)
# =============================================================================

# GitHub Integration OAuth (for syncing repos, profile data)
# Uses same OAuth App as auth, but different scopes
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# LinkedIn Integration OAuth (for syncing saved jobs)
# Create App: https://www.linkedin.com/developers/apps
# Callback URL: http://localhost:3000/api/auth/linkedin/callback
LINKEDIN_CLIENT_ID="your-linkedin-client-id"
LINKEDIN_CLIENT_SECRET="your-linkedin-client-secret"

# Encryption key for OAuth tokens stored in database
# Generate with: openssl rand -hex 32 (64-char hex string)
SOCIAL_ENCRYPTION_KEY="your-64-char-hex-encryption-key"

# Owner email for admin access (bootstrapping)
OWNER_EMAIL="your-email@example.com"

# =============================================================================
# AI CONFIGURATION
# =============================================================================

# AI Provider (choose one: gemini, openai, claude)
AI_PROVIDER="gemini"

# Google Gemini API (FREE - Recommended)
# Get free API key: https://aistudio.google.com/app/apikey
# Free tier: 15 requests/min, 1500 requests/day
GEMINI_API_KEY="your-gemini-api-key"

# OpenAI API (Optional - Paid)
# Get API key: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key"

# Anthropic Claude API (Optional - Paid)
# Get API key: https://console.anthropic.com/
ANTHROPIC_API_KEY="your-claude-api-key"

# =============================================================================
# VERCEL BLOB STORAGE (for CV files)
# =============================================================================

# Vercel Blob Storage - created automatically by Vercel
# Get token from Vercel Dashboard > Storage > Blob
BLOB_READ_WRITE_TOKEN="your-blob-token"

# =============================================================================
# MCP SERVERS (AI Development Tools)
# =============================================================================

# GitHub Personal Access Token (for GitHub MCP Server)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org, read:user, workflow
GITHUB_PERSONAL_ACCESS_TOKEN="ghp_your-token-here"

# Vercel API Token (for Vercel MCP Server)
# Create at: https://vercel.com/account/tokens
VERCEL_API_TOKEN="your-vercel-api-token"

# Brave Search API Key (for web search MCP - optional)
# Get free key: https://brave.com/search/api/
BRAVE_API_KEY="your-brave-api-key"
